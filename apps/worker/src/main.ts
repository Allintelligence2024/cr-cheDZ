/**
 * Worker asynchrone — squelette Phase 0/1.
 * Phase 5 : boucle de consommation de background_jobs (FOR UPDATE SKIP LOCKED),
 * handlers : send_parent_notification, generate_invoice_pdf, compress_media,
 * send_monthly_invoices, export_report. Voir docs/PLAN_EXECUTION_PROCHAINES_PHASES.md.
 */
// eslint-disable-next-line no-console
console.log('[worker] squelette prêt — handlers de jobs en Phase 5');
