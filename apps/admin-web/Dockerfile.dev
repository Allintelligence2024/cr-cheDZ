# Development only. Build context MUST be the monorepo root.
FROM node:22
WORKDIR /app
COPY . .
RUN npm ci
RUN npm run build --workspace @creche/prod-config
# Bind only source directories in Compose, never hide /app or node_modules.
CMD ["npm", "run", "dev", "--workspace", "@creche/admin-web"]
