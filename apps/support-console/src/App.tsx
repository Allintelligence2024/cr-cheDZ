/**
 * Squelette support-console (Phase 0).
 * Phase 10 : recherche globale, impersonation auditée, feature flags, jobs, DPO.
 */
export function App(): React.JSX.Element {
  return (
    <main style={{ fontFamily: 'system-ui, sans-serif', padding: 24 }}>
      <h1>Crèche SaaS — Console support</h1>
      <p>Accès restreint équipe interne. Plan : Phase 10.</p>
    </main>
  );
}
