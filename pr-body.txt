Reconstruction du P3 (commits précédents perdus avec leur sandbox, jamais poussés). Dockerfiles multi-stage 4 apps (contexte racine, prébuild prod-config, prune prod deps, USER node / nginx SPA). docker.yml : build sur PR sans push, push ghcr.io/allintelligence2024/creche-saas-* (latest+sha) sur main. flutter.yml : analyze continue-on-error documenté (issue #8). Les docker builds sont prouvés PAR cette CI (aucun build local possible si docker absent).
